# タスク: 画面上部のスクロール固定化 (Sticky Header)

## 1. アプリケーションコードの改修 (AI作業)
- [x] `Dashboard.tsx` の見出し・検索エリアを `<div className="sticky top-0 z-10 bg-gray-50 ...">` で囲む
- [x] `AdminBooks.tsx` の見出し・アクションボタン・検索エリアを `<div className="sticky top-0 z-10 bg-gray-50 ...">` で囲む

## 2. 動作確認 (ユーザー・AI作業)
- [ ] デスクトップ環境でスクロール時に両画面のヘッダーが固定されるか確認する
- [ ] スクロール時に下のコンテンツが透けて見えないか（背景色が効いているか）確認する
- [ ] モバイル（幅が狭い）環境でもレイアウトが崩れず固定されるか確認する
