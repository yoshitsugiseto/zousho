# タスクリスト (書籍管理と機能制限の改修)

- [x] `AdminBooks.tsx` の追加モーダルUIで、`relative z-20` などの適切なクラスを適用し、グレイアウト状態で操作不能となる不具合を修正する。
- [x] `AdminBooks.tsx` の書籍一覧の各行に「編集」ボタンを追加し、タイトル・著者・総冊数を変更できる編集フォームを作成する。
- [x] 冊数の増減に応じて、`book_items` の追加(insert)および削除(delete)が同期して行われる処理を実装する。（※貸出中のため削除できない場合は安全にガードする）
- [x] `Dashboard.tsx` において、一般ユーザーの場合は「本を返却する」UIを非表示または「返却は管理者へ」とする。
- [x] `Dashboard.tsx` において、管理者がアクセスした場合は自身での「本を借りる」アクションを非表示にする。
- [x] `AdminBooks.tsx` (または詳細機能) にて、貸出中の書籍と対象ユーザーを確認できるようにし、その機能から管理者が「返却処理」を実行できるようにする。
